version: '3'

services:
  nlpservice:
    build: .
    image: cornatul/nlp:v1
    restart: always
    ports:
      - "8000:8000"
    volumes:
      - type: bind
        source: .
        target: /app/
